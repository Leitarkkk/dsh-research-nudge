# Bundle layer discovered through package.json#dsh.bundle.patch.
- insert:
    - id: research-nudge
      name: dsh-research-nudge
      config:
        enabled: true
        debtThreshold: 20
        maxToolCallsWithoutResearch: 15
        maxMinutesWithoutResearch: 15
        cooldownMinutes: 10
